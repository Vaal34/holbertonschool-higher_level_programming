#!/usr/bin/node

const phrase = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (let i = 0; phrase[i]; i++) { console.log(phrase[i]); } // Affiche la valeur de `myVar` dans la console

/* peut être simplifié par :
    for (let value of phrase) {
        console.log(value);}
} */
